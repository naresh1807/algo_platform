# NOTE: the version of this file you uploaded back had been reduced to
# just 4 lines (asgiref, Django, sqlparse, tzdata) -- almost certainly
# an accidental `pip freeze > requirements.txt` overwrite at a point
# when the venv only had Django's own transitive deps installed, before
# the rest (DRF, channels, celery, pandas, etc.) were added. Restored
# the full list here. Your server clearly IS running with all of those
# installed (dashboard loads, WebSockets connect, login works) -- so
# your actual .venv is fine; it was specifically this FILE that was
# wrong. Keep it in sync with `pip freeze` going forward only after
# confirming the freeze includes everything, not before.

# Bumped to match what you're actually running successfully: Django
# 6.0.7 (confirmed via your own risk/migrations/0001_initial.py, whose
# header says "Generated by Django 6.0.7") -- the original <5.1 ceiling
# was written before 6.0 existed; since your system already proves 6.0
# works fine with this codebase, the pin now reflects reality instead
# of blocking your already-working setup.
Django>=5.2,<6.1
djangorestframework>=3.15
channels>=4.1
# channels-redis and redis (the standalone client, also used directly
# by Celery's broker/result backend below) must stay version-matched --
# channels-redis wraps redis-py's async client, and a redis-py release
# newer than what a given channels-redis version was built against can
# break at RUNTIME (import errors, dropped/renamed methods) rather than
# failing cleanly at `pip install` time, since channels-redis's own
# dependency bound is often looser than what actually works. Pinning
# BOTH to a range known to work together, rather than leaving redis
# open-ended, is the fix -- if you bump one, bump/test the other too.
channels-redis>=4.2,<5.0
redis>=4.5,<5.0
daphne>=4.1
mysqlclient>=2.2
celery>=5.4
# NOTE: pandas-ta (previously pinned here at ==0.3.14b0) has been
# removed entirely -- it hasn't had a real release since ~2021 and
# doesn't work correctly against Python 3.14 or pandas 3.0 (both
# released well after pandas-ta went quiet). Patching around its bugs
# one at a time (it also had a numpy.NaN import issue on modern numpy)
# stopped being worth it; apps/market_data/indicators.py now computes
# every indicator (EMA, MACD, RSI, ATR, ADX, Bollinger width, Parabolic
# SAR) directly with plain pandas/numpy operations instead, which are
# stable, actively-maintained APIs.
pandas>=2.2
requests>=2.32
python-dotenv>=1.0
django-cors-headers>=4.4
django-filter>=24.2

# Our own news-gathering system (apps/news/rss_client.py) -- parses real
# public RSS/Atom feeds and Google News RSS search results. Replaces the
# paid NewsAPI.org integration (apps/news/newsapi_client.py, kept
# unused/reference-only) as the default headline source; no API key
# needed.
feedparser>=6.0

# Field-level encryption utility (common/crypto.py) -- ready for any
# future model field that needs to store a secret in the DB. Not
# currently used by any model (see that module's docstring for why),
# but kept as a dependency since it's a tiny, stable stdlib-adjacent
# package (no heavy transitive deps) rather than something to add
# later under time pressure when it's actually needed.
cryptography>=42.0

# Broker integration (apps/market_data/broker_client.py). Only actually
# imported when BROKER_MODE=live -- see that module's lazy import note.
smartapi-python>=1.5.5
pyotp>=2.9
# smartapi-python imports this at runtime (SmartConnect's own logging
# setup) but doesn't declare it in its own install_requires -- without
# it, BrokerClient._connect() fails with ModuleNotFoundError the first
# time BROKER_MODE=live actually tries to log in, not at pip-install
# time.
logzero>=1.7
websocket-client>=1.7

# Sentiment analysis (apps/news/finbert.py). torch is CPU-only here
# deliberately -- this scaffold has no GPU-inference requirement (a
# handful of headlines every 5 minutes, not real-time high-volume
# scoring), and the CPU wheel is far smaller to install. Swap for a
# CUDA build only if you have a GPU AND profiling shows CPU inference
# is actually too slow for your polling interval.
transformers>=4.40
torch>=2.2

# Win-probability ML model (apps/learning/ml_train.py, ml_predict.py).
# scikit-learn only (no XGBoost/LightGBM) while trade counts are small
# -- see ml_train.py's module docstring for why a simple, interpretable
# model is the deliberate starting point, and where to add a bigger
# library later once there's enough closed-trade history to justify it.
scikit-learn>=1.4
joblib>=1.3
